apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopgpt-backend.fullname" . }}
  labels:
    {{- include "shopgpt-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopgpt-backend.selectorLabels" . | nindent 4 }}